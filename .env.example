# Copy this file to .env and fill in your key for local testing.
# In production (Render, etc.) set this as an environment variable in the dashboard instead —
# never commit a real key to your repo.
ANTHROPIC_API_KEY=sk-ant-your-key-here
INSTRUCTOR_PASSWORD=choose-a-password-here
PORT=3000
